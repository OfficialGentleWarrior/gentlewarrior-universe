// responses/open.js
// Gentle Heart — OPEN / DEFAULT RESPONSE

window.RESPONSES_OPEN = {
  entry: (lang) => ({
    text: lang === "en"
      ? "I’m here — what do you want to talk about?"
      : "Andito lang ako — ano ang gusto mong pag-usapan?",
    options: []
  })
};